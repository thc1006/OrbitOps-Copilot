---
description: Run the full test suite and report pending vs failed categories.
allowed-tools: Bash(./test.sh), Bash(./verify.sh), Bash(pytest *), Bash(npx vitest *), Read
---

Run `./test.sh`. If any hard failures, list them; if any PENDING categories, list which Sprint task will activate them. Report under 200 words.

Do NOT modify code — this is a read-only command.
