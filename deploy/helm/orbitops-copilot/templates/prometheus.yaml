{{- if .Values.prometheus.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "orbitops.fullname" . }}-prometheus
  labels:
    {{- include "orbitops.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: prometheus
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prometheus
        app.kubernetes.io/part-of: orbitops-copilot
    spec:
      containers:
        - name: prometheus
          image: "{{ .Values.prometheus.image.repository }}:{{ .Values.prometheus.image.tag }}"
          imagePullPolicy: {{ .Values.prometheus.image.pullPolicy }}
          args:
            - "--config.file=/etc/prometheus/prometheus.yml"
            - "--storage.tsdb.path=/prometheus"
            - "--storage.tsdb.retention.time={{ .Values.prometheus.tsdbRetention }}"
            - "--web.enable-lifecycle"
          ports: [{ containerPort: 9090, name: http }]
          volumeMounts:
            - { name: config, mountPath: /etc/prometheus, readOnly: true }
            - { name: data,   mountPath: /prometheus }
          resources:
{{ toYaml .Values.prometheus.resources | indent 12 }}
          livenessProbe:
            httpGet: { path: /-/healthy, port: 9090 }
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /-/ready, port: 9090 }
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        # NB: the ConfigMap `{{ .Values.prometheus.configMapName }}` is NOT
        # rendered by this chart. It's a checked-in
        # `deploy/k8s/base/prometheus-configmap.yaml` (referenced from
        # `kustomization.yaml` `resources:`, NOT generated by
        # `configMapGenerator`). For Helm-only deployments, apply that file
        # via `kubectl apply -f` (or render the kustomize base) before
        # `helm install`.
        - name: config
          configMap:
            name: {{ .Values.prometheus.configMapName }}
        - name: data
          emptyDir:
            sizeLimit: {{ .Values.prometheus.dataSizeLimit }}
---
apiVersion: v1
kind: Service
metadata:
  # ADR-009: bare Service name so Grafana's checked-in datasource ConfigMap
  # (`http://prometheus.orbitops.svc.cluster.local:9090`) resolves correctly.
  name: prometheus
  labels:
    {{- include "orbitops.labels" . | nindent 4 }}
spec:
  selector: { app.kubernetes.io/name: prometheus }
  ports:
    - { name: http, port: {{ .Values.prometheus.service.port }}, targetPort: 9090 }
{{- end -}}
